import styled from "@emotion/styled";

export const Container = styled.div`
  position: relative;
`;

export const Portal = styled.div`
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
`;
